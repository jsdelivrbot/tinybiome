[Unit]
Description=Tiny Biome Server
Requires=network.target
After=network.target

[Service]
ExecStart=/root/tb -master localhost:8000 node master files 2>&1 >> /var/log/tb.log
Type=simple
Restart=always

[Install]
WantedBy=multi-user.target