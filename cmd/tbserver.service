[Unit]
Description=Tiny Biome Server
Requires=network.target
After=network.target

[Service]
ExecStart=/root/tb -master localhost:8000 node master files
Type=forking
Restart=always

[Install]
WantedBy=multi-user.target